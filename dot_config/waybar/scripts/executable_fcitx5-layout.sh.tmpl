#!/bin/bash
# Generated by chezmoi for {{ .chezmoi.hostname }}

# Get the active fcitx5 input method name
layout=$(fcitx5-remote -n)

# Format the output for Waybar (Shorten long names if necessary)
case "$layout" in
"keyboard-us") echo "US" ;;
"pinyin") echo "CN" ;;
"mozc") echo "JP" ;;
*) echo "$layout" ;;
esac
